export type ROI = {
    x: number;
    y: number;
    w: number; // width
    h: number; // height
    id: string;
    timeStart: number; // start time in ms
    timeEnd: number; // end time in ms
};
